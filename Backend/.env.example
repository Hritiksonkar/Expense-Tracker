# Backend environment variables (copy to .env and fill real values)

# MongoDB Atlas (recommended: include an explicit database name after .net/)
# IMPORTANT:
# - Do NOT commit real credentials.
# - Copy the exact "Connect -> Drivers" connection string from Atlas.
# - Replace <db_user>, <db_password>, and <cluster_host>.
# - If your password has special chars (like @ : / ? # & %), URL-encode it.
MONGODB_URI=mongodb+srv://hritiksonkar:Hritik123@<cluster_host>/expense-tracker?retryWrites=true&w=majority

# Server
PORT=4444
NODE_ENV=development

# CORS (used in production mode)
# Example: https://your-frontend.vercel.app
# Tip: do NOT include a trailing slash.
FRONTEND_URL=http://localhost:5173


# Optional: email notifications (used by Backend/services/EmailService.js)
EMAIL=your_email@example.com
PASSWORD=your_email_app_password
ADMIN_EMAIL=admin_email@example.com
