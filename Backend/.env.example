# Backend environment variables (copy to .env and fill real values)

# MongoDB Atlas (recommended: include an explicit database name after .net/)
# Replace <db_password> with your real Atlas DB user password.
MONGODB_URI=mongodb+srv://hritiksonkar355_db_user:<db_password>@cluster0.vnsrcla.mongodb.net/expense-tracker?retryWrites=true&w=majority&appName=Cluster0

# Server
PORT=4444
NODE_ENV=development

# CORS (used in production mode)
# Example: https://your-frontend.vercel.app
FRONTEND_URL=http://localhost:5173

# Optional: email notifications (used by Backend/services/EmailService.js)
EMAIL=your_email@example.com
PASSWORD=your_email_app_password
ADMIN_EMAIL=admin_email@example.com
