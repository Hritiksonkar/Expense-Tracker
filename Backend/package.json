{
  "name": "expense-tracker-backend",
  "version": "1.0.0",
  "description": "Backend for Expense Tracker Application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon --ignore node_modules/ --trace-warnings index.js",
    "build": "npm install --production",
    "postbuild": "echo 'Backend build completed successfully'",
    "test": "echo 'No tests specified'",
    "clean": "rimraf node_modules package-lock.json",
    "setup": "npm install",
    "predev": "npm run setup",
    "heroku-postbuild": "npm install --production",
    "vercel-build": "npm install --production"
  },
  "keywords": [
    "expense",
    "tracker",
    "backend",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.4",
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "rimraf": "^5.0.10"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}